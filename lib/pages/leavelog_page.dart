import 'package:flutter/material.dart';

class LeaveLogPage extends StatelessWidget {
  const LeaveLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("LeaveLog page"),
      ),
    );
  }
}
